name: wallet
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  gsheets: 0.5.0
  flutter_lints: ^3.0.1

flutter:

  uses-material-design: true

 # assets:
   # - images/
